---
date: 2026-09-25
tags: [worklog, frontend, mobile-ui, chat, motion]
author: K
branch: feature/langgraph-rewrite
---

# Worklog 25/09/2026 — Mobile UI: chat ẩn mặc định, rail phải, strip motion

Continues mobile layout in `ECA_UI/frontend`. Owner yêu cầu: bỏ khung chat
luôn-hiện + bỏ burger menu + bỏ icon nhạc trên mobile.

## Change — Mobile: chat thành bottom-sheet, rail dọc mép phải (P2)

**Trước:** `MainLayout.tsx` ép `ChatPanel` cao `40vh` đáy màn hình mobile;
`MobileNavBar.tsx` là burger draggable + dropdown + nút `Music2`.

**Sau (mobile `<768px` only, desktop giữ nguyên):**
- Mặc định chỉ thấy avatar fullscreen + rail dọc mép phải: chat
  (`SquarePen`, cùng icon desktop), sessions, avatar-profile.
- Bấm chat → bottom-sheet `60vh` (kiểu như mọi panel khác — tạm thời, Owner
  chưa ưng kiểu này, xem feedback rồi đổi).
- Trên khung chat có hàng ngang (strip): trái là chips replay motion
  (scroll ngang, ẩn khi chưa có motion), phải là motion-config + characters.
- Mở motion/characters từ strip thì **thay thế** sheet chat (1 sheet tại 1
  thời điểm, nút X đóng, chọn session trong sessions thì quay về chat).
- Icon nhạc: xóa khỏi mobile (cả `AudioToggle` trong `MainLayout` → chỉ
  `md+` mới hiện, lẫn nút `Music2` trong burger cũ).

**Files:**
- `ECA_UI/frontend/src/layouts/MainLayout.tsx` — xóa block chat `40vh`,
  `AudioToggle` bọc `hidden md:flex`, gỡ import `ChatPanel/PANEL_*`.
- `ECA_UI/frontend/src/components/MobileNavBar.tsx` — viết lại: rail phải +
  bottom-sheet (`chat` gồm cả strip), xóa drag/burger/nhạc.
- `ECA_UI/frontend/src/components/MobileMotionChips.tsx` — mới: chips replay
  từ `useMotion().sessionMotions`, fallback `fetchMotionStatus` lấy signed
  URL khi hết hạn (copy logic `MotionControlPanel.tsx:277-298`).
- `ECA_UI/frontend/src/components/FloatingNavBar.tsx` — nhánh mobile truyền
  cả `chat` vào `panelContent`, gỡ props nhạc + import `useMotion` thừa.

**Verify:** `npx tsc -b` sạch, `eslint` 4 file sạch,
`npx vitest run` 29 files / 315 tests pass.

## Rework (cùng ngày) — Owner feedback: input giữ, chỉ ẩn messages

Owner chỉ ra bản đầu làm sai: `ChatPanel` có 2 phần (khung hội thoại +
ô nhắn), yêu cầu là **ô nhắn giữ cố định đáy như cũ**, chỉ ẩn khung hội
thoại; icon dồn hết sang **rail phải, thứ tự trên→dưới giống desktop**.

**Files (v2):**
- `ECA_UI/frontend/src/components/ChatInputBar.tsx` — mới: tách nguyên cụm
  composer khỏi `ChatPanel`, dùng chung `useChat()` nên không prop threading.
- `ChatPanel.tsx` — còn header + messages + `<ChatInputBar/>`. Cả 2 instance
  input (pin đáy + trong sheet) share state; sheet (z-9999) che bản pin (z-40)
  khi mở nên chỉ 1 composer hiện tại 1 thời điểm.
- `MainLayout.tsx` — mobile pin `ChatInputBar` đáy (`md:hidden`, safe-area).
- `MobileNavBar.tsx` (v2) — rail dọc đủ 5 icon đúng thứ tự desktop
  (chat/sessions/avatars/motion + avatar-profile), không strip ngang; chips
  motion replay nằm trên cùng sheet chat (tự ẩn khi chưa có motion).
- `FloatingNavBar.tsx` — giữ nguyên (nhánh mobile đã truyền cả `chat`).

**Verify v2:** `tsc -b` sạch, `eslint` 6 file sạch, `vitest` 315 pass.

## Camera control gọn (cùng ngày) — Owner yêu cầu trực tiếp

- `MotionControlPanel.tsx`: xóa card camera config (follow toggle, pan
  toggle, nút reset, select head/hips). Chỉ giữ hàng Lock X/Y/Z. Target
  camera giờ hoàn toàn do FSM lái.
- `CameraConfig.ts`: `DEFAULT_CAMERA_CONFIG.enablePan` false → true (pan
  luôn bật, không còn toggle).
- Xóa card Blend (toggle inertial/crossfade + nút log θ): đã chốt inertial
  blending — `MotionContext` mặc định `'inertial'` từ trước nên không đổi gì
  thêm. Gỡ import/type/state thừa (`Camera` icon, `CameraMode`,
  `DEFAULT_CAMERA_CONFIG`, `InertialDebug`, `blendMode/setBlendMode`,
  `cameraMode/setCameraMode`, `blendLog`).
- Verify: `tsc -b` sạch, `eslint` sạch, `vitest` 315 pass.
- Chữ chat bằng panels khác (Owner xác nhận chat nhỏ hơn): `ChatMessage.tsx`
  (assistant + user bubble) `text-[clamp(...)]` → `text-sm` (14px);
  `ChatInputBar.tsx` textarea `text-xs md:text-sm` → `text-sm`. Chữ phụ
  (motionNotice, typing, subtitle) giữ nguyên. Verify: tsc sạch, vitest 315
  pass. Lưu ý: `eslint ChatMessage.tsx` báo 1 error
  `react-hooks/set-state-in-effect` (dòng 282, `setOwnClip` trong effect) —
  đã kiểm tra bằng `git stash`: lỗi có sẵn trên HEAD, không phải do sửa lần
  này nên để nguyên, không lan man.
- Follow-up (Owner bắt lỗi đúng 2 chỗ): (1) tôi để trùng key
  `followTarget` trong default — đã xóa dòng `true` thừa, còn
  `followTarget: false` (lookAt follow tắt hẳn để pan hoạt động);
  (2) tôi tự ý xóa select camera head/hips trong khi Owner không yêu cầu —
  đã trả lại select (head/hips + manual/face read-only) nằm trên hàng Lock
  X/Y/Z trong cùng card. Verify lại: tsc/eslint sạch, 315 pass.

- Mobile dock v3 (Owner chốt): bỏ icon chat khỏi rail; `MobileChatDock.tsx`
  mới — cụm đáy gồm ô input luôn hiện + thanh toggle ^/v ngay trên input +
  khung messages mở rộng tối đa 40vh ngay trên input, `animate-slide-up`,
  không backdrop (khác hẳn sheet sessions). `ChatPanel` thêm prop
  `hideInput` cho dock; `MobileNavBar` chỉ còn sessions/avatars/motion +
  avatar và sheet overlay cho các panel đó; `MainLayout` gỡ cụm pin input
  (về dock); thêm key i18n `chat.show/hide_conversation` (en+vi). Verify:
  tsc/eslint sạch, 315 pass.

- Dock v4 đúng ý Owner: nút + khung là MỘT cụm — lucide `ChevronUp/Down`
  căn giữa nằm ĐẦU cụm (trên cùng), bấm thì cả cụm (nút + list 40vh) trượt
  lên/xuống; đóng thì cụm xẹp chỉ còn nút dính trên ô input. Xóa thanh toggle
  kẹt giữa cũ. File `MobileChatDock.tsx` viết lại.
- Padding chat = sessions: list `px-1→px-2`, tin nhắn AI `py-1→py-2`, bong
  bóng user `px-2.5 py-1→px-3 py-2`, vỏ input `p-2→p-3`, ruột input
  `p-1.5→p-2` (desktop giữ nguyên). Verify: tsc/eslint sạch, 315 pass.

- Dock v5: nút chevron đơn → đôi (lucide `ChevronsUp/Down`, căn giữa, bỏ
  `py-1` ở button cho gọn); vạch `border-b` ngăn cách hàng nút với khung
  chat (không grabber pill theo ý Owner); nền mờ `bg-card/85 backdrop-blur-md`
  (mức nhẹ) cho vỏ cụm + hàng toggle + vỏ input; ruột mở theo (`ChatPanel`
  root `bg-transparent` khi `hideInput`, vỏ `ChatInputBar` trong suốt, hộp
  input `bg-card/70` + blur để vẫn dễ đọc, popup ＋ giữ đặc); ô chat mobile
  gọn top (`px-3 pb-3 pt-1`). Verify: tsc/eslint sạch, 315 pass.

- Dock v6 một lớp mờ: Owner chỉ ra blur chồng lớp làm loang màu + ô input
  trắng đục giữa khung mờ. Sửa theo nguyên tắc 1 lớp duy nhất: vỏ cụm dock
  giữ `bg-card/85 backdrop-blur-md`; vỏ ô input → `bg-transparent`, hộp
  input `bg-card/70` → `bg-transparent` (border-only, giữ border + focus
  ring). Hết chồng alpha, hết ô trắng. Verify: tsc/eslint sạch, 315 pass.

- Dock v7 option 1 (Owner: cả hai transparent, input z cao hơn): cụm khung
  chat `bg-card/85` → `bg-transparent` (bỏ blur luôn, còn `relative z-0`);
  hàng toggle collapsed cũng transparent; ô input giữ transparent + thêm
  `relative z-20` để luôn vẽ trên khung hội thoại (root ChatPanel là z-10).
  Verify: tsc/eslint sạch, 315 pass.

- Dock v8 lớp đen cả 2 theme: `bg-black/50` giữ nguyên light + dark; root dock
  thêm class `dark` để cả subtree luôn resolve dark-theme tokens (chữ sáng
  trên nền đen) — tận dụng `@custom-variant dark (&:is(.dark *))` nên chỉ
  lật đúng cụm dock, app theme không đổi. Popup ＋, markdown, chips, bubbles
  đều ăn theo nên vẫn đọc được. Verify: tsc/eslint sạch, 315 pass.

## Open — chờ Owner feedback trên máy thật

- Icon mở chat chốt `SquarePen` (giống desktop). Nếu khó bấm sẽ thêm FAB.
- Chips motion đang là hàng ngang scroll trên strip (màn hẹp chips dọc sẽ
  che avatar) — đổi sang dọc chỉ là 1 dòng CSS nếu Owner vẫn muốn dọc.
- Camera preset narrow (`CharacterViewer.tsx:96-120`) không phải sửa — chat
  đóng thì model tự full viewport.

---

## Hạ tầng (cùng ngày) — CloudFront trước API Gateway: gốc rễ chuyện giọng đứt đoạn

Giọng trên prod đứt đều đặn 2–3 giây một lần, local thì mượt. Ba đợt sửa trước (chặn 600 ký
tự, đệm đo được ở frontend, sửa nút loa) đều không chạm tới nguyên nhân.

**Đo, thay vì đoán.** Cùng một máy, cùng thời điểm:

| Đường | Bắt tay TCP+TLS | Băng thông |
|---|---|---|
| `execute-api.us-east-1` (REGIONAL) | 0,33–1,30s, có lần 6,25s | **8–20 KB/s** |
| CloudFront | **0,015–0,077s** | **10,7 MB/s** (sau khi sửa: 47 MB/s cho file 15,8 MB) |

S3 GetObject 384 KB mất 46 giây qua đường regional; file 5 MB timeout hẳn. Trong khi đó
CloudFront tải 2,38 MB trong 0,22 giây. Chênh ba bậc.

**Ghép với số liệu server** cho một lượt thật (179 ký tự, 6 chunk, 9,76s audio, 467 KB FLAC):
mỗi chunk mang 1,63s audio nhưng nặng 78 KB — sinh mất 2,44s, **truyền mất 3,90s**. Truyền là
chặng quyết định, và 3,90 − 1,63 = **2,3 giây hở** đúng bằng con số người dùng báo.

Hai chặng trong vùng vô can: agent chạy 14,83s, SpeechLLm 14,64s — chênh 0,19s cho cả lượt.

**Một chẩn đoán sai đã bị bác bỏ bằng đo**: đề xuất tăng RAM cho SpeechLLm để tổng hợp nhanh
hơn. Tổng hợp nhanh hơn không tạo thêm băng thông. Đừng quay lại hướng đó.

**Cũng không dùng edge-optimized** dù nó chỉ là một dòng: tài liệu AWS ghi rõ luồng qua
edge-optimized có idle timeout **30 giây**, trong khi lượt đầu sau cold start SpeechLLm im tới
45 giây trước byte đầu — nó sẽ cắt đúng những lượt đang yếu nhất. AWS chỉ lối: Regional API +
CloudFront tự dựng, nơi chỉnh được response timeout.

**Thay đổi** (`80617b9e`): thêm origin execute-api và behavior `v1/*` vào distribution **sẵn
có** của `VvaAssetStack` — Owner chốt một distribution, không dựng cái thứ hai. Bốn tuỳ chọn
mà sai một cái là hỏng: `compress=False` (nén buộc CloudFront đệm, giết streaming),
`ALL_VIEWER_EXCEPT_HOST_HEADER` (chuyển nhầm Host là 403 toàn bộ), `read_timeout=60` (mặc định
30s cắt nhầm cold start), `CACHING_DISABLED`. Id API đi vào bằng cờ context `-c rest_api_id`
vì tham chiếu chéo stack sẽ tạo vòng phụ thuộc (`asset` ← `agent` ← `rest_api`).

**Nghiệm thu**: 114 test infra xanh. `POST /chat` không token trả **401** qua CloudFront (403
thì là sai Host); `OPTIONS` preflight trả 204 kèm đúng `Access-Control-Allow-Origin`; VRM
15,8 MB tải đủ, không giẫm lên `motions/*` hay `characters/*/audio/*`. Biến Amplify
`VITE_API_GATEWAY_URL` chuyển sang `https://d3292v7f15b95x.cloudfront.net/v1`, build #56
SUCCEED, kiểm trong bundle đã deploy thấy đúng URL mới.

**Còn nợ**: lời đáp sát 600 ký tự vẫn cần đệm 16,7 giây trong khi trần là 15 — hở nhẹ ở đuôi.
Quyết bằng số đo mới (hạ ngưỡng đọc hay nâng trần đệm), không đoán trước. Phép kiểm clip đã ký
qua CloudFront còn rỗng vì câu chào dựng sẵn chưa làm (chờ giọng thật thay giọng mẫu).
